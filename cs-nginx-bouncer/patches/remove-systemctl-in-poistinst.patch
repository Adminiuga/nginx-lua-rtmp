diff --git a/debian/postinst b/debian/postinst
--- a/debian/postinst
+++ b/debian/postinst
@@ -1,5 +1,6 @@
+#!/bin/sh
 
-systemctl daemon-reload
+set -e
 
 luarocks install lua-resty-http 0.17.1-0
 luarocks install lua-cjson 2.1.0.10-1
@@ -12,9 +13,7 @@ LAPI_DEFAULT_PORT="8080"
 
 if [ "$1" = "configure" ]; then
 
-    type cscli
-
-    if [ "$?" -eq "0" ] ; then
+    if `command -v cscli` ; then
         # Check if it's an upgrade
         if [ "$2" != "" ] ; then
             echo "Upgrading, check if there is bouncer configuration"
diff --git a/debian/postrm b/debian/postrm
--- a/debian/postrm
+++ b/debian/postrm
@@ -1,3 +1,6 @@
+#!/bin/sh
+set -e
+
 if [ "$1" = "remove" ]; then
     rm /etc/nginx/conf.d/crowdsec_nginx.conf || echo "Unable to remove file '/etc/nginx/conf.d/crowdsec_nginx.conf'. Remove it manually or your NGINX service will not be able to restart."
-fi
\ No newline at end of file
+fi
